// src/main/java/com/judith/aplicacionweb/proyectooptica/api/RestApplication.java
package com.judith.aplicacionweb.proyectooptica.api;

import java.util.HashSet;
import java.util.Set;

/*import jakarta.ws.rs.ApplicationPath;
import jakarta.ws.rs.core.Application;


/**
 * Clase que define la aplicación JAX-RS y la ruta base para todos los servicios RESTful.
 * Todos los endpoints REST comenzarán con /api/.
 */
//@ApplicationPath("/api")
public class RestApplication {
        //extends Application

    /* (Opcional) Puedes registrar manualmente los recursos aquí, o dejar que el contenedor lo escanee.
    @Override
    public Set<Class<?>> getClasses() {
        Set<Class<?>> classes = new HashSet<>();
        // classes.add(com.judith.aplicacionweb.proyectooptica.api.resources.CitaResource.class);
        return classes;
    }*/
}
